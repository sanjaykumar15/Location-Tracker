package com.sanjay.trackLocation.Database
/*
import android.app.Application
import io.realm.Realm
import io.realm.RealmConfiguration

class RealmDbCon: Application() {

    private var config: RealmConfiguration?=null

    override fun onCreate() {
        super.onCreate()
        Realm.init(this)
        config = RealmConfiguration.Builder().name("trackLocation.db").deleteRealmIfMigrationNeeded().schemaVersion(0).allowWritesOnUiThread(true).build()
        config.let {
            if (it != null) {
                Realm.setDefaultConfiguration(it)
            }
        }
    }

}*/
